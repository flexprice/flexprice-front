# Dark-mode audit

Generated by walking the running app in dark mode with a contrast scanner, plus a static severity
sweep over the source. Re-run either at any time; both are described at the bottom.

## Status

186 tokens, ~2,600 replacements across 300 files, light mode verified byte-identical on every commit.

|                   | At audit | Now   |
| ----------------- | -------- | ----- |
| CRITICAL          | 515      | **0** |
| HIGH              | 285      | **0** |
| MEDIUM            | 433      | **0** |
| Files with issues | 189      | **0** |

Those numbers only ever counted **named palette classes**, because that is all the original sweep
knew how to look for. Three later passes each found a whole category the previous one had been blind
to — arbitrary hex, inline styles, then `rgb()` longhand — so the honest claim is not "complete" but
"every mechanism we can enumerate is covered, and each is now guarded":

| Mechanism                                  | Status                              |
| ------------------------------------------ | ----------------------------------- |
| Named palette classes (`bg-white`)         | guarded — `CLASS_RE`                |
| Arbitrary hex classes (`bg-[#fafafa]`)     | guarded — `ARBITRARY_RE`            |
| Inline style hex (`color: '#64748B'`)      | guarded — `INLINE_RE`               |
| Inline style `rgb()`/`rgba()` longhand     | guarded — `INLINE_RGB_RE`           |
| SVG `fill=` / `stroke=` attributes         | guarded — `SVG_ATTR_RE`             |
| `src/index.css` own utility rules          | fixed, **not** guarded              |
| `shadow-[…rgba(0,0,0,…)]`                  | left literal — see below            |

Black drop shadows are deliberately untouched. They simply stop reading on a dark surface, which is
conventional; the dark theme separates by value instead (`--popover` `#252528` over `--card`
`#1d1d1f` over `--background` `#161617`), so nothing depends on the shadow for elevation.

Sixteen files keep literal colours on purpose — each sits on a surface that does not change between
themes, so tokenizing them would break the component. They are listed with reasons in
`scripts/verify-no-raw-palette.mjs`, which fails the build if a raw class reappears anywhere else.

The customer portal and checkout are excluded by design; see below.

## What the audit originally found

Measured across 189 files, 1,491 occurrences — all now resolved.

| Severity     | Occurrences | What it looks like in dark                                     | Top offenders                                                                                          |
| ------------ | ----------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| **CRITICAL** | 515         | Dark text on dark, or a white panel punched into the dark page | `bg-white` (145), `text-gray-900` (84), `bg-zinc-100` (50), `text-zinc-900` (43), `text-zinc-950` (40) |
| **HIGH**     | 285         | Readable but well under AA                                     | `text-gray-600` (105), `text-gray-700` (70), `text-zinc-600` (30), `bg-gray-200` (27)                  |
| **MEDIUM**   | 433         | Muted text and hairlines that wash out                         | `text-gray-500` (114), `border-gray-200` (91), `text-zinc-500` (65)                                    |
| **LOW**      | 258         | Status/accent colours that survive but are not tuned           | `text-blue-600` (19), `text-white` (19), `text-red-500` (15)                                           |

### Worst files (CRITICAL×3 + HIGH)

| File                                                   | C   | H   | M   |
| ------------------------------------------------------ | --- | --- | --- |
| `components/molecules/PricingCard/PricingCard.tsx`     | 19  | 19  | 36  |
| `pages/customer/tabs/CustomerAnalyticsTab.tsx`         | 18  | 22  | 10  |
| `pages/onboarding/PricingSetupPage.tsx`                | 22  | 8   | 17  |
| `components/customer-portal/InvoicesTab.tsx`           | 20  | 2   | 7   |
| `components/customer-portal/UsageAnalyticsTab.tsx`     | 16  | 13  | 9   |
| `pages/insights-tools/revenue/Revenue.tsx`             | 15  | 16  | 20  |
| `pages/insights-tools/exports/ExportDetails.tsx`       | 12  | 11  | 1   |
| `components/molecules/Dashboard/InvoiceIssuesCard.tsx` | 15  | 1   | 4   |
| `pages/checkout/CheckoutPage.tsx`                      | 14  | 4   | 11  |
| `pages/settings/team/UsersSection.tsx`                 | 9   | 5   | 25  |

> These were worked in the order listed and are all resolved. The customer-portal and checkout rows
> were **excluded from the migration** — see _Customer portal is deliberately not migrated_ below.

## Customer portal is deliberately not migrated

`/customer-portal` and `/checkout` are **public routes**, outside `MainLayout` and `AuthMiddleware`.
The viewer is the tenant's customer, not a Flexprice user.

They already have their own theming contract: `PortalConfigContext` injects `--portal-primary`,
`--portal-bg`, `--portal-surface`, `--portal-border` from per-tenant settings, and derives text
colours automatically from the configured background via `isColorDark()`. A tenant can already
configure a dark portal. Layering `.dark` on top would be a second, competing mechanism fighting the
tenant's brand.

**This surfaced a real defect.** `initTheme()` runs on every route, so `.dark` was being applied to
those public pages — and because they compose shared atoms that _are_ tokenized (`Button`, `Input`,
`Table`, `ui/card`), the result was dark buttons and tables inside an otherwise white,
tenant-branded page. Measured on `/checkout`: `bg-surface` resolved to `#1d1d1f` while the
surrounding `bg-white` chrome stayed `#ffffff`.

Fixed by scoping dark mode to the authenticated app:

- `useThemeStore` recognises `PUBLIC_LIGHT_ROUTES` and never applies `.dark` there, so first paint
  is correct.
- `useForceLightTheme()` (used by `CustomerPortalWrapper` and `CheckoutPage`) strips the class on
  mount and restores the persisted preference on unmount, covering client-side navigation from
  inside the app.

Verified both directions: `/checkout` stays light with `dark` persisted, `/home` still goes dark.
Seven tests cover it, including that a lookalike path like `/checkout-settings` is _not_ treated as
public.

## Token fixes already applied

### `lib/typography.ts` — the single highest-leverage file

Every heading, label, body, table-header and breadcrumb style in the app resolves through
`getTypographyClass()`, and all 26 colour classes in it were hardcoded `text-zinc-*`. That is why
page headings rendered dark-on-dark at **1.18:1**. One file, 26 replacements, and every heading in
the product became readable. Home went from 7 contrast failures to 4.

### Four content tokens failed AA in dark

Measured every content token against the three common dark surfaces (`surface` #1d1d1f,
`surface-shell` #0f0f10, `surface-subtle` #232326):

| Token                  | Was       | Now       | Worst ratio before → after       |
| ---------------------- | --------- | --------- | -------------------------------- |
| `content-subtle`       | `#6e727b` | `#878c95` | 3.25 → 4.70                      |
| `content-zinc-subtle`  | `#6e727b` | `#878c95` | 3.25 → 4.70                      |
| `content-slate-subtle` | `#74787f` | `#858b96` | 3.53 → 4.63                      |
| `content-disabled`     | `#55585f` | unchanged | 2.20 — **left alone on purpose** |

`content-disabled` is meant to read as disabled, and WCAG exempts disabled controls. Brightening it
would make disabled fields look enabled.

**19 of 20 content tokens now clear AA in dark.** These were dark-only edits; light is byte-identical
and `verify:light` confirms it.

### `surface-inverse-zinc` added

`RevenueTrendCard`'s tooltip was `bg-zinc-900 text-white`. The text migrated to `content-inverse`
(which flips to near-black in dark) but `bg-zinc-900` had no bg-role token, so it stayed dark — dark
text on a dark tooltip. `surface-inverse-zinc` (zinc.900 → `#eeeff1`) inverts together with
`content-inverse`, the same pairing already used for the gray-based tooltips.

This is the third instance of the same trap (switch thumb, nightOwl code surface, now this): **the
question is never "what hex is this", it is "what does this sit on, and does that change".**

### The environment badge was light-on-light in dark (found in Step 16)

The sandbox/production pill in the sidebar sets its background through an **inline gradient of
hardcoded pastels** (`#FFFCEE → #FFF9DD` for sandbox, `#EEF4FF → #DDE7FF` for production). Step 13
tokenized its _text_ to `accent-yellow-deep`, which correctly flips light in dark mode — but the
gradient underneath stayed a pale pastel. The result was light text on a light pill, on a Midnight
sidebar.

This is the same trap as the switch thumb and the nightOwl code surface, and this time the
migration walked into it: **a token that inverts must not be paired with a surface that does not.**
Inline `style` backgrounds are the easy place to miss, because the class-based tooling never sees
them.

Six tokens now carry the badge (`env-dev-bg`, `env-dev-bg-mid`, `env-prod-bg`, `env-prod-bg-mid`,
`env-prod-line`, `env-prod-text`) so text and surface move together:

|                          | Light (unchanged) | Dark (before → after)              |
| ------------------------ | ----------------- | ---------------------------------- |
| sandbox text on badge    | 8.43 / 8.19       | light-on-light → **11.79 / 10.60** |
| production text on badge | 5.38 / 4.80       | light-on-light → **8.35 / 7.36**   |

### Text over a background photo (found in final review)

The login page's right panel sets a `backgroundImage` — a light sky photograph, identical in both
themes. Its tagline and "Trusted by" heading had been tokenized to `content-zinc` / `content-black`,
which correctly flip to near-white in dark. On a pale photo that is invisible.

Every earlier trap scan looked for `style={{ background` and `text-white`; none looked for a
`backgroundImage` on an **ancestor**, so this survived to the final visual pass. It was found by
looking at the screen, not by any tool.

Swept every file containing `backgroundImage`, `bg-cover` or `bg-[url(` afterwards. The rest are
safe for a reason worth stating: they use the photo as a _backdrop_ and put content inside a nested
`bg-surface` card, so text and its surface invert together. `LandingSection` was the only place text
sat directly on the image.

This is the fifth instance of the one rule that governs the whole migration: **a token that inverts
must not be paired with a surface that does not.** Its forms so far — a switch thumb on an inverting
track, a permanently-dark code surface, an inline gradient, a scrim, and now a photograph.

## Known non-issues

Things that look broken but are not:

- **Active sidebar item has no background in light.** shadcn's `data-[active=true]:bg-sidebar-accent`
  outranks the item's own class and resolves to an undefined variable in light. Pre-existing on
  `main`; only the border shows. In dark it correctly picks up the Midnight value.
- **Sidebar promo card missing.** Renders only when the environment has no plans and no features.
- **Light chips are below AA** (3.95–4.48). Pre-existing design, reproduced byte-for-byte. Fixing it
  is a visible light change and needs its own PR.
- **Dark is measurably more legible than light.** Across the 22 token pairs the source states on a
  single element, light fails AA on five and dark on none. The worst — `text-content-subtle` on
  `bg-surface-sidebar` — reads 2.41:1 in light and 5.67:1 in dark. Worth knowing before anyone reads
  a dark-mode contrast complaint as a regression.

## The six guards

All run in the pre-commit hook. Each exists because something slipped past the others.

| Guard                   | Catches                                                                                               |
| ----------------------- | ----------------------------------------------------------------------------------------------------- |
| `verify:theme`          | a token's light value drifting from its Tailwind source                                               |
| `verify:light`          | a class mapped to the **wrong** token — passes `verify:theme`, still changes light                    |
| `verify:tokens-staged`  | committing `src/index.css` without `tailwind.config.js`, which ships a variable with no utility class |
| `verify:exportable`     | the widget package shipping `var(--fp-*)` it never defines                                            |
| `verify:no-raw-palette` | a raw palette class reappearing in themed code                                                        |
| `verify:dark-contrast`  | a token pair that passes AA in light but fails in dark                                                |

`verify:dark-contrast` deliberately does **not** enforce absolute AA. Light is frozen and already
fails AA on five of the 22 stated pairs, so an absolute bar could only be met by editing light. It
asserts the invariant this migration can own — dark never breaks a pair that worked in light — and
ignores headroom lost above the threshold, since 21:1 to 16.65:1 is arithmetic rather than a defect.

Only same-element pairs are checked. Text inheriting a background from an ancestor is not resolvable
statically, so a clean run is a claim about every pair the source states explicitly, not about the
whole app.

Every one was negative-tested — deliberately broken, confirmed to fail, restored. Three of them had
bugs that only surfaced that way: `verify:light` counted class names inside comments,
`verify:exportable` was satisfied by the dark scope alone and separately deleted its own markers
while stripping comments. A guard that has never been seen to fail is not evidence of anything.

## Re-running the audits

```bash
npm run verify:theme && npm run verify:light && npm run verify:tokens-staged \
  && npm run verify:exportable && npm run verify:no-raw-palette
```

For the in-browser contrast scan, open the app in dark mode and paste the scanner from this
workstream's session notes — it walks every text node, resolves the first opaque ancestor
background, and reports anything under 4.5:1 (3:1 for large text), grouped by class.
